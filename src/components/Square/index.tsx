import { FC } from 'react';
import { SquareContainer, Symbol } from "./styles";

const Square: FC = () => {
  return (
    <SquareContainer>
        <Symbol>X</Symbol>
    </SquareContainer>
  );
}

export default Square;
